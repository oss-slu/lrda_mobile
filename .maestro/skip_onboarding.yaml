appId: register.edu.slu.cs.oss.lrda
---
# Skip the onboarding carousel (only appears on first launch).
# Taps "Skip" if visible, otherwise assumes already onboarded.
- runFlow:
    when:
      visible: "Skip"
    commands:
      - tapOn: "Skip"
